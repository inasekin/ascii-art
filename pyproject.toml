[project]
name = "ascii-art"
version = "0.1.0"
description = "Конвертер изображений в ASCII-арт"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow>=11.0.0",
]

[project.scripts]
ascii-art = "main:main"
